{
  "name": "MetaProxy",
  "version": "0.1.0",
  "author": "pinkiebell",
  "license": "MPL-2.0",
  "type": "module",
  "scripts": {
    "compile": "develatus-apparatus-compile contracts/**/*.sol contracts/*.sol",
    "coverage": "yarn compile && develatus-apparatus",
    "test": "yarn _test \"test/**/*.test.js\"",
    "_test": "yarn compile && mocha --file test/mocha.js --color --bail --exit --timeout=900000 $@"
  },
  "devDependencies": {
    "develatus-apparatus": "https://github.com/pinkiebell/develatus-apparatus.git#v0.3.3",
    "ethers": "=5.0.31",
    "mocha": "=7.1.1",
    "solc": "=0.7.6"
  }
}
